name: Analyze Theme Structure

on:
  push:
    branches: [main]

jobs:
  analyze-theme:
    runs-on: ubuntu-latest
    steps:
    - name: تحميل الكود
      uses: actions/checkout@v3

    - name: تحليل هيكل القالب
      run: |
        echo "📁 تحليل هيكل القالب الموجود..."
        
        # البحث عن ملفات ووكومرس
        echo "🔍 البحث عن ملفات ووكومرس:"
        find . -path "*/woocommerce/*" -name "*.php" | head -10 || echo "⚪ لم يتم العثور على مجلد woocommerce مخصص"
        
        # البحث عن ملفات المنتجات
        echo "📦 البحث عن ملفات المنتج:"
        find . -name "*product*" -type f | grep -E "\.(php|css|js)$" | head -10
        
        # تحليل هيكل السمات
        echo "🎨 تحليل هيكل السمات:"
        if [ -d "wp-content/themes" ]; then
            find wp-content/themes/ -name "style.css" -exec grep -l "Theme Name" {} \;
        fi
        
        # إنشاء تقرير بالهيكل الحالي
        echo "📊 تقرير الهيكل الحالي:"
        find . -maxdepth 3 -type d -not -path "./.git/*" -not -path "./.github/*" | sort

